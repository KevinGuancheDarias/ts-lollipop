/**
 * Represents the type that a controller can "produce" or "consume"
 *
 * @export
 * @enum {number}
 * @since 0.1.0
 */
export enum MediaTypeEnum {
    NONE = 'none',
    JSON = 'json',
    TEXT = 'text',
    HTML = 'html'
}
