/**
 * Represents the possible types of modules
 *
 * @export
 * @enum {number}
 */
export enum ModuleTypes {
    OTHER = 0,
    CONTROLLER = 1,
    DI = 2,
    DATABASE = 3,
    CONTROLLER_SECURITY = 4
}
